angular
    .module('zakaz-xd.auth.login-form', [
    ])
    .controller('LoginFormCtrl', ['$scope', '$stateParams', '$state',
        function ($scope, $stateParams, $state) {
        }
    ])
;
